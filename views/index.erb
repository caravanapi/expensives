<!DOCTYPE HTML>
<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8">
    <title>Revista Fuças - A primeira Revista de fofocas Nerd</title>
  </head>
  <body>
    <h1>Revista Fuças</h1>
    <img src="images/DSC00278.jpg" alt="Fábio Akita volta pra casa arrastado depois do #horaextra do #oxenterails" />
    <p class="legend">Fábio Akita volta pra casa arrastado depois do #horaextra do #oxenterails</p>
    
    <% @posts.each do |post| %>
      <ul class="post">
        <li>
          <h2><%= post.title %></h2>
          <img src="<%= post.image_url %>" title="<%= post.title %>" />
          <p><%= post.subtitle%></p>
        </li>
      </ul>
    <% end %>
  </body>
<html>
